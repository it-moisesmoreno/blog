// Fig. 2
#include <iostream>

int main() {
    int count = 5;

    // Use the while statement for iterative counting
    while (count > 0) {
        std::cout << count << " ";
        count--;
    }

    std::cout << "Liftoff!" << std::endl;

    return 0;
}
